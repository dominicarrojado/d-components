export const WINDOW_ANIMATION_MS = 200;
export const WINDOW_TITLE_BAR_DRAG = 'title-bar-drag';

export enum WINDOW_BTNS {
  SETTINGS = 'settings',
  MINIMIZE = 'minimize',
  MAXIMIZE = 'maximize',
  RESTORE = 'restore',
  CLOSE = 'close',
}
export enum WINDOW_STATES {
  RESTORED = 'restored',
  MINIMIZED = 'minimized',
  MAXIMIZED = 'maximized',
  CLOSED = 'closed',
}
